import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lingoquiz.app',
  appName: 'lingoquiz',
  webDir: 'public',
  server: {
    url: 'https://lingoquiz-app-1073935551759.us-central1.run.app'
  }
};

export default config;
